import { NoPropietarioPipe } from './no-propietario.pipe';

describe('NoPropietarioPipe', () => {
  it('create an instance', () => {
    const pipe = new NoPropietarioPipe();
    expect(pipe).toBeTruthy();
  });
});
